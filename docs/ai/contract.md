# 📜 Code Contract

## Запреты

- ❌ `as Type` — unsafe assertion
- ❌ `any`
- ❌ `!` — non-null assertion
- ❌ Избыточные комментарии, дублирующие очевидный код
- ❌ Inline-стили в JSX (используй CSS Modules или классы)
- ❌ `useEffect` для derived state (вычисляй в рендере или `useMemo`)
- ❌ `index` как `key` в динамических списках
- ❌ Прямая мутация state / props

## Обязательно

- ✅ Early return для невалидных случаев
- ✅ Optional chaining (`?.`, `??`)
- ✅ try/catch для async операций
- ✅ Guard clauses перед работой с данными
- ✅ Проверка границ массивов перед доступом
- ✅ Компоненты — function declaration (`function Component()`, не `const Component = () =>`)
- ✅ Хуки — в отдельных файлах с префиксом `use` (`useExpenses.ts`)
- ✅ Типы пропсов — `interface ComponentProps` рядом с компонентом
- ✅ Единственный `export default` — для компонента; именованные экспорты — для хуков, утилит и типов

## React Best Practices

- Предпочитай композицию, а не пропс-дриллинг (Context API для глобального состояния)
- `useMemo` / `useCallback` — только при доказанной необходимости, не преждевременно
- Ленивая загрузка (`React.lazy` + `Suspense`) для тяжёлых роутов
- Controlled components для форм
- Error Boundaries для изоляции сбоев

## Тесты

- Покрываем бизнес-логику, не фреймворк
- Не тестируем тривиальные обёртки и pass-through компоненты
- Приоритет: функции с условиями, валидация, расчёты (конвертация периодов!)
- Названия на русском, описывают что проверяется
- Структура: Arrange → Act → Assert
- Одна проверка на тест
- Моки: только localStorage и внешние зависимости

## Проверка

```bash
pnpm lint && pnpm test
```

Сдача без прохождения — запрещена.

## См. также

- `docs/ai/style-guide/text.md` — правила текста
- `docs/ai/style-guide/code.md` — стиль кода
